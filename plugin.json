{
  "name": "zcode-mcp-bridge",
  "version": "0.3.0",
  "description": "Event-driven ZCode scheduling bridge for Codex: concurrent resource-aware runs, replayable observation, recovery, background control, branching, context compaction, and lifecycle management.",
  "license": "MIT",
  "mcpServers": {
    "zcode-agent": {
      "type": "stdio",
      "command": "python3",
      "args": ["${ZCODE_PLUGIN_ROOT}/server.py"],
      "env": {
        "ZCODE_MCP_TIMEOUT": "900"
      }
    }
  }
}
