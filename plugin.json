{
  "name": "coder-mcp-bridge",
  "version": "0.5.0-dev",
  "description": "Event-driven MCP bridge for orchestrating ZCode, OpenCode and Pi through a unified agent toolset.",
  "license": "MIT",
  "mcpServers": {
    "vibe_bridge": {
      "type": "stdio",
      "command": "python3",
      "args": ["${ZCODE_PLUGIN_ROOT}/server.py"],
      "env": {
        "AGENT_MCP_TIMEOUT": "900"
      }
    }
  }
}
